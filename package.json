{
  "name": "colourway",
  "version": "1.0.0",
  "description": "Colourway application",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "test": "vitest run --pool=threads",
    "test:coverage": "vitest run --coverage --pool=threads"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^5.0.1",
    "jsdom": "^30.1.1",
    "vitest": "^5.0.1"
  }
}
